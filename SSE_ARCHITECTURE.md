# 🏗️ SSE Architecture Diagram

## 📊 System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         SSE NOTIFICATION SYSTEM                  │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────┐                           ┌──────────────────┐
│   FRONTEND       │                           │    BACKEND       │
│   (React)        │                           │    (Django)      │
└──────────────────┘                           └──────────────────┘
```

---

## 🔄 Connection Flow

```
┌─────────────┐                                    ┌─────────────┐
│   Browser   │                                    │   Django    │
│   Client    │                                    │   Server    │
└──────┬──────┘                                    └──────┬──────┘
       │                                                  │
       │  1. Open SSE Connection                         │
       │  GET /api/notifications/sse/?token=JWT          │
       ├────────────────────────────────────────────────>│
       │                                                  │
       │  2. Validate JWT Token                          │
       │                                                  ├─────┐
       │                                                  │     │
       │                                                  │<────┘
       │                                                  │
       │  3. Create Queue for User                       │
       │                                                  ├─────┐
       │                                                  │     │ user_queues[user_id] = []
       │                                                  │<────┘
       │                                                  │
       │  4. Keep Connection Open (200 OK)               │
       │<─────────────────────────────────────────────────┤
       │  Content-Type: text/event-stream                │
       │                                                  │
       │  5. Ping every 30s                              │
       │<─────────────────────────────────────────────────┤
       │  data: {"type": "ping"}                         │
       │                                                  │
       │  6. When notification arrives                   │
       │<─────────────────────────────────────────────────┤
       │  data: {"type": "notification", ...}            │
       │                                                  │
       │  7. Update UI immediately                       │
       ├─────┐                                            │
       │     │ Refresh notifications                     │
       │<────┘                                            │
       │                                                  │
```

---

## 🏛️ Backend Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         DJANGO BACKEND                           │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  users/views.py                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Global State (Thread-Safe)                            │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │  user_queues = {}  # {user_id: [Queue1, Queue2, ...]} │    │
│  │  queue_lock = Lock()                                   │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  send_notification_to_user(user_id, data)              │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │  1. Acquire lock                                       │    │
│  │  2. Get all queues for user_id                         │    │
│  │  3. Put data into each queue                           │    │
│  │  4. Release lock                                       │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  NotificationSSEView (APIView)                         │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │  def get(request):                                     │    │
│  │    1. Validate JWT token from query string             │    │
│  │    2. Create Queue for this connection                 │    │
│  │    3. Add queue to user_queues[user_id]                │    │
│  │    4. Return StreamingHttpResponse                     │    │
│  │    5. Loop:                                            │    │
│  │       - Check queue for messages                       │    │
│  │       - Send message if available                      │    │
│  │       - Send ping every 30s                            │    │
│  │    6. Cleanup queue on disconnect                      │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  TriggerNotificationView (APIView)                     │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │  def post(request):                                    │    │
│  │    1. Get user_id, title, message from request         │    │
│  │    2. Call send_notification_to_user()                 │    │
│  │    3. Return success response                          │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  users/urls.py                                                   │
├─────────────────────────────────────────────────────────────────┤
│  path("notifications/sse/", NotificationSSEView.as_view())      │
│  path("notifications/trigger/", TriggerNotificationView...)     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🎨 Frontend Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         REACT FRONTEND                           │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  services/sseService.js                                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  class SSEManager (Singleton)                          │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │  Properties:                                           │    │
│  │    - eventSource: EventSource | null                   │    │
│  │    - listeners: Set<Function>                          │    │
│  │    - reconnectTimeout: number | null                   │    │
│  │                                                        │    │
│  │  Methods:                                              │    │
│  │    - connect(userId)                                   │    │
│  │      * Create EventSource                              │    │
│  │      * Set up event handlers                           │    │
│  │      * Handle reconnection                             │    │
│  │                                                        │    │
│  │    - disconnect()                                      │    │
│  │      * Close EventSource                               │    │
│  │      * Clear reconnect timeout                         │    │
│  │                                                        │    │
│  │    - addListener(callback)                             │    │
│  │      * Add callback to listeners set                   │    │
│  │                                                        │    │
│  │    - removeListener(callback)                          │    │
│  │      * Remove callback from listeners set              │    │
│  │                                                        │    │
│  │    - notifyListeners(data)                             │    │
│  │      * Call all registered callbacks                   │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  utils/notificationHelper.js                                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  triggerNotificationUpdate(userId, notificationData)   │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │  POST /api/notifications/trigger/                      │    │
│  │  Body: { user_id, title, message, type }               │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  addNotification(notification)                         │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │  1. Get notifications from localStorage                │    │
│  │  2. Add new notification                               │    │
│  │  3. Save to localStorage                               │    │
│  │  4. Trigger SSE update                                 │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  Layout/Header/UserActions.jsx                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  useEffect(() => {                                     │    │
│  │    // 1. Initial load                                  │    │
│  │    fetchNotifications();                               │    │
│  │                                                        │    │
│  │    // 2. Connect SSE                                   │    │
│  │    if (userId) {                                       │    │
│  │      sseManager.connect(userId);                       │    │
│  │      sseManager.addListener(handleSSEUpdate);          │    │
│  │    }                                                   │    │
│  │                                                        │    │
│  │    // 3. Fallback: Window focus                        │    │
│  │    window.addEventListener("focus", onFocus);          │    │
│  │                                                        │    │
│  │    // 4. Fallback: Storage events (multi-tab)          │    │
│  │    window.addEventListener("storage", onStorage);      │    │
│  │                                                        │    │
│  │    // 5. Cleanup                                       │    │
│  │    return () => {                                      │    │
│  │      sseManager.removeListener(handleSSEUpdate);       │    │
│  │      sseManager.disconnect();                          │    │
│  │      window.removeEventListener("focus", onFocus);     │    │
│  │      window.removeEventListener("storage", onStorage); │    │
│  │    };                                                  │    │
│  │  }, [userId]);                                         │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow

### 1. Initial Connection

```
User opens app
     ↓
UserActions.jsx mounts
     ↓
useEffect runs
     ↓
sseManager.connect(userId)
     ↓
EventSource created
     ↓
GET /api/notifications/sse/?token=JWT
     ↓
Django validates token
     ↓
Queue created for user
     ↓
Connection established (200 OK)
     ↓
Ping messages start (every 30s)
```

### 2. Notification Delivery

```
Event occurs (e.g., new order)
     ↓
Backend calls send_notification_to_user(user_id, data)
     ↓
Data pushed to all queues for user_id
     ↓
NotificationSSEView reads from queue
     ↓
Sends SSE message to client
     ↓
EventSource receives message
     ↓
sseManager.notifyListeners(data)
     ↓
handleSSEUpdate() called
     ↓
fetchNotifications() executed
     ↓
UI updates with new notification
     ↓
Badge count increases
```

### 3. Auto-Reconnect

```
Connection lost (network issue, server restart)
     ↓
EventSource fires 'error' event
     ↓
sseManager detects error
     ↓
Logs: "Connection lost, reconnecting in 5s..."
     ↓
Wait 5 seconds
     ↓
sseManager.connect(userId) again
     ↓
New EventSource created
     ↓
Connection re-established
     ↓
Notifications resume
```

### 4. Multi-Tab Synchronization

```
Tab 1: User marks notification as read
     ↓
localStorage updated: notif_read_{id} = true
     ↓
'storage' event fired
     ↓
Tab 2: Receives 'storage' event
     ↓
Tab 2: onStorage() handler called
     ↓
Tab 2: fetchNotifications() executed
     ↓
Tab 2: UI updates with read status
```

---

## 🔐 Security Flow

```
┌─────────────┐
│   Client    │
└──────┬──────┘
       │
       │ 1. User logs in
       ├────────────────────────────────────────────────────>
       │                                                      
       │ 2. Receives JWT token                               
       │<─────────────────────────────────────────────────────
       │                                                      
       │ 3. Stores token in localStorage                     
       ├─────┐                                                
       │     │ localStorage.setItem('token', jwt)            
       │<────┘                                                
       │                                                      
       │ 4. Opens SSE connection with token                  
       │ GET /api/notifications/sse/?token=JWT               
       ├────────────────────────────────────────────────────>
       │                                                      
       │                                    5. Validates JWT  
       │                                    ┌────────────────┤
       │                                    │                │
       │                                    └───────────────>│
       │                                                      
       │ 6. Connection established if valid                  
       │<─────────────────────────────────────────────────────
       │                                                      
       │ 7. Receives notifications                           
       │<─────────────────────────────────────────────────────
       │                                                      
```

---

## 📊 Queue Management

```
┌─────────────────────────────────────────────────────────────────┐
│                         user_queues                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  user_id: 1                                                      │
│    ├─ Queue 1 (Tab 1, Chrome)                                   │
│    ├─ Queue 2 (Tab 2, Chrome)                                   │
│    └─ Queue 3 (Mobile Safari)                                   │
│                                                                  │
│  user_id: 2                                                      │
│    ├─ Queue 1 (Tab 1, Firefox)                                  │
│    └─ Queue 2 (Tab 2, Firefox)                                  │
│                                                                  │
│  user_id: 3                                                      │
│    └─ Queue 1 (Tab 1, Edge)                                     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

When notification sent to user_id: 1
     ↓
send_notification_to_user(1, data)
     ↓
Acquire queue_lock
     ↓
Get all queues for user_id: 1
     ↓
Put data into Queue 1 ✓
Put data into Queue 2 ✓
Put data into Queue 3 ✓
     ↓
Release queue_lock
     ↓
All 3 connections receive notification simultaneously
```

---

## 🎯 Performance Comparison

### Before (Polling)

```
Time: 0s ──────> 2s ──────> 4s ──────> 6s ──────> 8s
       │         │         │         │         │
       ├─ GET ──┤         │         │         │
       │         ├─ GET ──┤         │         │
       │         │         ├─ GET ──┤         │
       │         │         │         ├─ GET ──┤
       │         │         │         │         ├─ GET ──>

Requests: 5 in 8 seconds
Latency: 0-2s (average 1s)
Server load: Constant
```

### After (SSE)

```
Time: 0s ──────────────────────────────────────────────────> 60s
       │                                                      │
       ├─ CONNECT ────────────────────────────────────────> │
       │    ↓                                                │
       │    └─ PUSH (when event occurs) <100ms               │
       │                                                      │
       └─ PING (30s) ──────────────────────> PING (60s) ──> │

Requests: 1 connection + 2 pings in 60 seconds
Latency: <100ms
Server load: Minimal (idle most of time)
```

---

## 🚀 Scalability

### Single Server

```
┌─────────────┐
│   Django    │
│   Server    │
│             │
│ user_queues │ ← In-memory dictionary
│   {1: [...]}│
│   {2: [...]}│
│   {3: [...]}│
└─────────────┘
```

### Multi-Server (Future with Redis)

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  Django 1   │     │  Django 2   │     │  Django 3   │
└──────┬──────┘     └──────┬──────┘     └──────┬──────┘
       │                   │                   │
       └───────────────────┼───────────────────┘
                           │
                    ┌──────┴──────┐
                    │    Redis    │
                    │   Pub/Sub   │
                    └─────────────┘

When notification sent:
1. Django 1 publishes to Redis channel
2. All Django servers subscribe to channel
3. Each server pushes to its connected clients
4. All users receive notification regardless of server
```

---

## 📈 Monitoring Points

```
┌─────────────────────────────────────────────────────────────────┐
│                         MONITORING                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Backend:                                                        │
│    ✓ Number of active SSE connections                           │
│    ✓ Number of queues per user                                  │
│    ✓ Queue size (messages waiting)                              │
│    ✓ Connection duration                                        │
│    ✓ Ping success rate                                          │
│    ✓ Error rate                                                 │
│                                                                  │
│  Frontend:                                                       │
│    ✓ Connection status (connected/disconnected)                 │
│    ✓ Reconnection attempts                                      │
│    ✓ Message receive latency                                    │
│    ✓ Listener count                                             │
│                                                                  │
│  Infrastructure:                                                 │
│    ✓ Server CPU usage                                           │
│    ✓ Memory usage                                               │
│    ✓ Network bandwidth                                          │
│    ✓ Request count                                              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🎓 Key Design Decisions

### 1. Why SSE over WebSocket?
- ✅ Simpler (one-way communication)
- ✅ Native browser support (EventSource)
- ✅ Automatic reconnection
- ✅ HTTP-based (easier with proxies)
- ✅ No need for bidirectional communication

### 2. Why Queue per connection?
- ✅ Supports multiple tabs/devices per user
- ✅ Independent message delivery
- ✅ Easy cleanup on disconnect
- ✅ No message loss

### 3. Why JWT in query string?
- ✅ EventSource doesn't support custom headers
- ✅ Simple authentication
- ✅ Stateless
- ✅ Works with existing auth system

### 4. Why 30s ping interval?
- ✅ Keeps connection alive
- ✅ Not too frequent (low overhead)
- ✅ Not too slow (detects disconnects quickly)
- ✅ Standard practice

### 5. Why fallback mechanisms?
- ✅ Resilience (if SSE fails)
- ✅ Multi-tab sync (storage events)
- ✅ Catch-up on focus (user returns)
- ✅ Better UX

---

**Architecture designed by Team K23 🚀**