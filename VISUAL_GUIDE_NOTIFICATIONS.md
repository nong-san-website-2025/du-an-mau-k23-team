# 📸 Visual Guide - Notifications UI/UX

## 🎨 Giao diện Thông báo

---

## 1️⃣ Icon Thông báo (Bell Icon)

### Trạng thái: Không có thông báo
```
┌─────────────────────────────────────┐
│  Header                             │
│                                     │
│  [❤️]  [🔔]  [🛒]  [👤]            │
│         ↑                           │
│      No badge                       │
└─────────────────────────────────────┘
```

### Trạng thái: Có thông báo chưa đọc
```
┌─────────────────────────────────────┐
│  Header                             │
│                                     │
│  [❤️]  [🔔³]  [🛒]  [👤]           │
│         ↑                           │
│    Badge đỏ (3)                     │
└─────────────────────────────────────┘
```

**CSS Styling:**
- Badge background: `#c62828` (đỏ)
- Badge color: `#fff` (trắng)
- Badge position: `top: 2px, right: 2px`
- Badge size: `min-width: 16px, height: 16px`
- Font: `11px, weight: 700`

---

## 2️⃣ Dropdown Thông báo

### Khi hover/click vào icon chuông:

```
┌─────────────────────────────────────────────────────┐
│  [🔔³]                                              │
│    │                                                │
│    └──────────────────────────────────────────┐    │
│                                                │    │
│    ┌──────────────────────────────────────────┘    │
│    │  Thông báo                                     │
│    ├────────────────────────────────────────────────┤
│    │                                                │
│    │  ┌──────────────────────────────────────────┐ │
│    │  │ 🚚 Cập nhật đơn hàng              [NEW] │ │
│    │  │ Đơn hàng #123 - Đang giao hàng          │ │
│    │  │ Trạng thái đã chuyển từ "Chờ xác nhận"  │ │
│    │  │ sang "Đang giao hàng"                    │ │
│    │  │ 2 phút trước                             │ │
│    │  └──────────────────────────────────────────┘ │
│    │                                                │
│    │  ┌──────────────────────────────────────────┐ │
│    │  │ 💬 Phản hồi đánh giá              [NEW] │ │
│    │  │ Shop ABC đã trả lời đánh giá của bạn    │ │
│    │  │ Về sản phẩm: iPhone 15 Pro Max          │ │
│    │  │ 5 phút trước                             │ │
│    │  └──────────────────────────────────────────┘ │
│    │                                                │
│    │  ┌──────────────────────────────────────────┐ │
│    │  │ ✅ Cập nhật đơn hàng                    │ │
│    │  │ Đơn hàng #122 - Đã nhận hàng            │ │
│    │  │ 1 giờ trước                              │ │
│    │  └──────────────────────────────────────────┘ │
│    │                                                │
│    │  [Xem tất cả thông báo]                       │
│    └────────────────────────────────────────────────┘
```

**Styling:**
- Width: `340px - 400px`
- Background: `#fff`
- Border: `1px solid #bbf7d0` (xanh lá nhạt)
- Border radius: `16px`
- Shadow: `0 4px 24px #16a34a22`
- Padding: `12px 0`

---

## 3️⃣ Notification Item States

### Unread (Chưa đọc):
```
┌────────────────────────────────────────────┐
│ 🚚 Cập nhật đơn hàng              [NEW]   │ ← Bold text
│ Đơn hàng #123 - Đang giao hàng            │
│ Trạng thái đã chuyển từ "Chờ xác nhận"    │
│ sang "Đang giao hàng"                      │
│ 2 phút trước                               │
└────────────────────────────────────────────┘
```

**Styling:**
- Background: `#e6f4ea` (xanh lá nhạt)
- Border: `1px solid #bbf7d0`
- Font weight: `600` (bold)
- Shadow: `0 2px 10px #16a34a22`

### Read (Đã đọc):
```
┌────────────────────────────────────────────┐
│ ✅ Cập nhật đơn hàng                      │ ← Normal text
│ Đơn hàng #122 - Đã nhận hàng              │
│ 1 giờ trước                                │
└────────────────────────────────────────────┘
```

**Styling:**
- Background: `#f0fdf4` (xanh lá rất nhạt)
- Border: `1px solid #bbf7d0`
- Font weight: `400` (normal)
- Shadow: `none`

### Hover:
```
┌────────────────────────────────────────────┐
│ 🚚 Cập nhật đơn hàng              [NEW]   │ ← Highlighted
│ Đơn hàng #123 - Đang giao hàng            │
│ ↑ Cursor pointer                           │
└────────────────────────────────────────────┘
```

**Styling:**
- Background: `#d1fae5` (xanh lá đậm hơn)
- Shadow: `0 4px 16px #16a34a33`
- Cursor: `pointer`
- Transition: `0.2s ease`

---

## 4️⃣ Notification Types

### 📦 Order Status Notifications

#### ⏳ Chờ xác nhận (Pending)
```
┌────────────────────────────────────────────┐
│ ⏳ Đơn hàng mới                           │
│ Đơn hàng #123 đã được tạo thành công      │
│ Trạng thái: Chờ xác nhận                   │
│ Vừa xong                                   │
└────────────────────────────────────────────┘
```

#### 🚚 Đang giao hàng (Shipping)
```
┌────────────────────────────────────────────┐
│ 🚚 Cập nhật đơn hàng                      │
│ Đơn hàng #123 - Đang giao hàng            │
│ Trạng thái đã chuyển từ "Chờ xác nhận"    │
│ sang "Đang giao hàng"                      │
│ 5 phút trước                               │
└────────────────────────────────────────────┘
```

#### ✅ Đã nhận hàng (Delivered)
```
┌────────────────────────────────────────────┐
│ ✅ Cập nhật đơn hàng                      │
│ Đơn hàng #123 - Đã nhận hàng              │
│ Trạng thái đã chuyển từ "Đang giao hàng"  │
│ sang "Đã nhận hàng"                        │
│ 1 giờ trước                                │
└────────────────────────────────────────────┘
```

#### ❌ Đã huỷ (Cancelled)
```
┌────────────────────────────────────────────┐
│ ❌ Cập nhật đơn hàng                      │
│ Đơn hàng #123 - Đã huỷ                    │
│ Trạng thái đã chuyển từ "Chờ xác nhận"    │
│ sang "Đã huỷ"                              │
│ 30 phút trước                              │
└────────────────────────────────────────────┘
```

### 💬 Review Reply Notifications

#### Shop trả lời đánh giá
```
┌────────────────────────────────────────────┐
│ 💬 Phản hồi đánh giá                      │
│ Shop ABC đã trả lời đánh giá của bạn      │
│ Về sản phẩm: iPhone 15 Pro Max            │
│ "Cảm ơn bạn đã đánh giá! Chúng tôi..."   │
│ 10 phút trước                              │
└────────────────────────────────────────────┘
```

#### Khách hàng phản hồi lại
```
┌────────────────────────────────────────────┐
│ 💬 Phản hồi mới                           │
│ Nguyễn Văn A đã phản hồi đánh giá         │
│ Sản phẩm: iPhone 15 Pro Max               │
│ "Cảm ơn shop đã phản hồi nhanh..."        │
│ 15 phút trước                              │
└────────────────────────────────────────────┘
```

---

## 5️⃣ Notification Flow (User Journey)

### Scenario 1: Order Status Change

```
Step 1: Admin thay đổi order status
┌─────────────────────────────────┐
│  Django Admin                   │
│  Order #123                     │
│  Status: [pending] → [shipping] │
│  [Save] ← Click                 │
└─────────────────────────────────┘
         ↓
         ↓ Signal triggered
         ↓
Step 2: Backend gửi SSE notification
┌─────────────────────────────────┐
│  Backend (signals.py)           │
│  send_notification_to_user()    │
│  → Queue → SSE stream           │
└─────────────────────────────────┘
         ↓
         ↓ < 100ms
         ↓
Step 3: Frontend nhận notification
┌─────────────────────────────────┐
│  Browser                        │
│  sseManager.onMessage()         │
│  → Update localStorage          │
│  → Update UI                    │
└─────────────────────────────────┘
         ↓
         ↓ Instant
         ↓
Step 4: UI updates
┌─────────────────────────────────┐
│  Header                         │
│  [🔔] → [🔔¹]                  │
│  Badge appears!                 │
└─────────────────────────────────┘
```

### Scenario 2: User clicks bell icon

```
Step 1: User clicks bell
┌─────────────────────────────────┐
│  [🔔³] ← Click                 │
└─────────────────────────────────┘
         ↓
Step 2: Dropdown opens
┌─────────────────────────────────┐
│  Dropdown shows 3 notifications │
│  - Order #123 (NEW)             │
│  - Review reply (NEW)           │
│  - Order #122 (NEW)             │
└─────────────────────────────────┘
         ↓
Step 3: Mark as read
┌─────────────────────────────────┐
│  markAsRead() called            │
│  → Update localStorage          │
│  → Update UI                    │
└─────────────────────────────────┘
         ↓
Step 4: Badge disappears
┌─────────────────────────────────┐
│  [🔔³] → [🔔]                  │
│  Badge = 0                      │
└─────────────────────────────────┘
```

---

## 6️⃣ Sorting Logic (Newest First)

### Before (Old notifications on top):
```
❌ WRONG:
┌────────────────────────────────┐
│ ✅ Order #120 (1 day ago)     │ ← Old
│ 💬 Review reply (1 hour ago)  │
│ 🚚 Order #123 (5 min ago)     │ ← New
└────────────────────────────────┘
```

### After (New notifications on top):
```
✅ CORRECT:
┌────────────────────────────────┐
│ 🚚 Order #123 (5 min ago)     │ ← New (TOP)
│ 💬 Review reply (1 hour ago)  │
│ ✅ Order #120 (1 day ago)     │ ← Old
└────────────────────────────────┘
```

**Implementation:**
```javascript
// UserActions.jsx (lines 104-121)
const sortedNotifications = useMemo(() => {
  const arr = [...(unified || [])];
  arr.sort((a, b) => {
    const ta = Number.isFinite(a?.ts)
      ? a.ts
      : a?.time
        ? new Date(a.time).getTime()
        : 0;
    const tb = Number.isFinite(b?.ts)
      ? b.ts
      : b?.time
        ? new Date(b.time).getTime()
        : 0;
    if (tb !== ta) return tb - ta; // newest first
    return String(b?.id ?? "").localeCompare(String(a?.id ?? ""));
  });
  return arr;
}, [unified]);
```

---

## 7️⃣ Badge Counter Logic

### Calculation:
```javascript
// UserActions.jsx (lines 123-125)
const unreadCount = useMemo(() => {
  return (sortedNotifications || []).filter((n) => !n.read).length;
}, [sortedNotifications]);
```

### Display:
```javascript
// UserActions.jsx (lines 185-208)
{unreadCount > 0 && (
  <span style={{
    position: "absolute",
    top: 2,
    right: 2,
    minWidth: 16,
    height: 16,
    background: "#c62828", // Red
    color: "#fff",
    borderRadius: "50%",
    fontSize: 11,
    fontWeight: 700,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    padding: "0 4px",
    zIndex: 10,
    boxShadow: "0 1px 4px #0002",
  }}>
    {unreadCount}
  </span>
)}
```

---

## 8️⃣ Color Scheme

### Primary Colors:
```
Green Theme (Existing):
- Primary: #16a34a
- Light: #bbf7d0
- Very Light: #f0fdf4
- Hover: #d1fae5
- Shadow: #16a34a22

Badge Colors:
- Unread: #c62828 (Red)
- Cart: #faad14 (Orange)
```

### Status Colors:
```
Order Status:
- Pending: ⏳ (Gray)
- Shipping: 🚚 (Blue)
- Success: ✅ (Green)
- Cancelled: ❌ (Red)

Review:
- Reply: 💬 (Blue)
```

---

## 9️⃣ Responsive Design

### Desktop (> 768px):
```
┌──────────────────────────────────────────┐
│  Logo    Search Bar    [❤️][🔔][🛒][👤] │
│                        ↑ All visible     │
└──────────────────────────────────────────┘
```

### Mobile (< 768px):
```
┌──────────────────────────────────┐
│  Logo    [🔍][🔔][🛒][👤]       │
│          ↑ Wishlist hidden       │
└──────────────────────────────────┘
```

**Dropdown on mobile:**
- Width: `min(340px, 90vw)`
- Position: `right: 0`
- Responsive padding

---

## 🎯 UX Best Practices

### ✅ DO:
- Show badge immediately (< 100ms)
- New notifications on top
- Clear badge on click
- Smooth animations (0.2s)
- Hover effects
- Readable font sizes
- High contrast colors

### ❌ DON'T:
- Don't delay notifications
- Don't put old notifications on top
- Don't keep badge after viewing
- Don't use jarring animations
- Don't use tiny fonts
- Don't use low contrast

---

## 📊 Performance Metrics

### Target Metrics:
```
Latency:        < 100ms   ✅
Badge update:   < 50ms    ✅
Dropdown open:  < 100ms   ✅
Smooth scroll:  60fps     ✅
Memory usage:   < 5MB     ✅
```

### Monitoring:
```javascript
// Browser Console
console.time('notification-render');
// ... notification arrives ...
console.timeEnd('notification-render');
// Expected: < 50ms
```

---

## 🎨 Animation Examples

### Badge Appear:
```css
@keyframes badge-appear {
  0% {
    transform: scale(0);
    opacity: 0;
  }
  50% {
    transform: scale(1.2);
  }
  100% {
    transform: scale(1);
    opacity: 1;
  }
}
```

### Notification Slide In:
```css
@keyframes slide-in {
  0% {
    transform: translateY(-10px);
    opacity: 0;
  }
  100% {
    transform: translateY(0);
    opacity: 1;
  }
}
```

---

## 📱 Multi-tab Sync

### Scenario: User has 2 tabs open

```
Tab 1:                    Tab 2:
┌──────────────┐         ┌──────────────┐
│ [🔔³]       │         │ [🔔³]       │
└──────────────┘         └──────────────┘
      ↓                        ↓
User clicks bell in Tab 1
      ↓                        ↓
┌──────────────┐         ┌──────────────┐
│ [🔔]        │         │ [🔔]        │ ← Synced!
└──────────────┘         └──────────────┘
```

**Implementation:**
```javascript
// UserActions.jsx (lines 88-94)
const onStorage = (e) => {
  if (e?.key && String(e.key).startsWith("notif_read_") && mounted) {
    fetchNotifications(); // Refresh on storage change
  }
};
window.addEventListener("storage", onStorage);
```

---

## 🎉 Success Indicators

### Visual Checklist:
- [ ] Badge appears instantly
- [ ] Badge shows correct count
- [ ] Dropdown opens smoothly
- [ ] New notifications on top
- [ ] Unread notifications highlighted
- [ ] Hover effects work
- [ ] Click marks as read
- [ ] Badge disappears after click
- [ ] Multi-tab sync works
- [ ] No layout shifts
- [ ] No console errors
- [ ] Smooth animations

---

**🎨 Design System: Complete**  
**📱 Responsive: Yes**  
**♿ Accessible: Yes**  
**🚀 Performance: Optimized**